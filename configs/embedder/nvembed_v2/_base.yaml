# Configuration for NVEmbed V2 model

# Local directory name for model storage
local_dirname: "nvembed_v2"

# Remote server port
remote_port: 8101

# Vector dimension
vector_dim: 1024

# Model-specific settings
model_settings:
  model_name: "nvidia/nvembed-v2"
  batch_size: 32
  max_length: 512

